language: android
sudo: false

after_success:
  - bash <(curl -s https://codecov.io/bash)

android:
  components:
    - android-28
    - tools
    - build-tools-28.0.1
    
os:
  - linux
  
jdk:
  - oraclejdk8
  
script:
  - chmod +x ./gradlew
